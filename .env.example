# Copy to .env and export, or set in your shell.
# Without a key the app runs in demo mode (canned candidates, full pipeline).
ANTHROPIC_API_KEY=
# Optional overrides:
# SL_MODEL=claude-sonnet-5
# SL_DB_PATH=./sl.db
